---
title: 计网学习笔记
cover: https://yuumii.top/article/tencentIp/Tencentip.png
categories: 学习笔记
tags:
  - 计网
keywords: 计网学习笔记
ai: >-
  雨眠AI摘要帮您理解这篇文章~本文复习计算机网络OSI模型，对比七层国际标准与四层TCP/IP事实标准，详解各层功能，强调会话层、表示层在互联网中被合并到应用层，并介绍TCP/IP网络接口层、网际层、运输层和应用层核心协议。
date: 2026-07-02 11:12:06
description:
---

# 计网学习笔记

（更新中）

## 前言
重新回来复习一遍计网，复习一下 顺便当做博客也可以记录记录吧

## OSI模型
OSI模型主要是有两个 一个是七层一个四层，七层是国际标准，四层是事实标准，互联网真实使用的是四层模型，也就是TCP/IP模型
1. OSI 七层体系结构（法律上的国际标准）
物理层：传输比特流（0/1 信号），定义线缆、电压、接口规范
数据链路层：将比特封装成帧，相邻节点传输，MAC 寻址、差错检测
网络层：跨网络路由寻址，IP 地址、分组转发
运输层：端到端进程通信，TCP/UDP，流量 / 拥塞控制
会话层：建立、管理、断开通信会话（互联网几乎不用）
表示层：数据加密、压缩、编码转换（互联网几乎不用）
应用层：面向用户应用程序（浏览器、微信、FTP 等）
考点：会话层、表示层在互联网 TCP/IP 体系中被合并到应用层，日常网络设备极少单独实现这两层。
2. TCP/IP 四层体系结构（事实上的国际标准，互联网真实使用）
网络接口层
无统一标准，兼容以太网、WiFi、PPP 等各类底层网络
等价合并 OSI 的物理层 + 数据链路层
路由器、网卡硬件主要工作层
网际层（互联网层）
核心协议：IP（Everything over IP）
实现跨网络寻址、分组路由转发
运输层
TCP：可靠、面向连接；UDP：无连接、高效
应用层
包含 OSI 应用层、表示层、会话层全部功能
常见协议：HTTP、DNS、FTP、SMTP、RTP
 
这里推荐一篇文章https://xiaolincoding.com/network/1_base/tcp_ip_model.html

 ## TCP和UDP
TCP的叫传输控制协议(Transmission Control Protocol)，我们现在的大部分应用使用就是TCP，常见的比如HTTP应用层协议。TCP相比UDP多了很多特性，比如流量控制、超时重传、拥塞控制等，这些都是为了保证数据包能可靠地传输给对方。
UDP相对来说就很简单，简单到只负责发送数据包，不保证数据包是否能抵达对方，但它实时性相对更好，传输效率也高，因为UDP没有连接过程，直接发送数据包即可。当然 UDP也可以实现可靠传输，把TCP的特性在应用层上实现就可以。














### IP协议和IP地址
IP协议是网络层的一个重要的协议，协议会将传输层的包稳作为数据部分，再加上IP包头组装成IP报文（如果太大则需要分片 1500字节 也就是MTU）这样就有了一个可以发送到网络的IP报文
IP地址是一个很重要的东西，决定了在网络中能不能找到你，对于IPV4来说，IP地址为32位分为4段，每段是8位 
IP地址一般有两种意义，一个是网络号 一个是主机号，网络号是用于区分不同的网络，也就是负责标识该IP地址是属于哪一个“子网”的，主机号是用于区分网络中的主机，区分的方法需要配合一下子网掩码，
![怎么区分网络号和主机号](https://yuumii.top/article/%E8%AE%A1%E7%BD%91/%E6%80%8E%E4%B9%88%E5%8C%BA%E5%88%86IP%E5%9C%B0%E5%9D%80.png)